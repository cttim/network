two programs are java program

Make version is GNU Make 3.81

compiler version is javac 1.6.0_27

1 Run the make command to compile two java programs

2 Run the server first in the format of "java server"

3 After running the server, it will print the n_port num

4 RUn the client now in the format of "java client 127.0.0.1 n_port "sentence""
      n_port the the num print by server
      sentence is the sentence you want to reverse

 
